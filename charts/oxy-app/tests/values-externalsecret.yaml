externalSecrets:
  create: true
  storeRef:
    name: my-store
    kind: SecretStore
  envSecretNames:
    - my-env-secret
  envSecretMappings:
    my-env-secret:
      DATABASE_URL: /prod/database/url
  fileSecrets:
    - name: my-file-secret
      key: bigquery-key.json
      path: my/file/path
